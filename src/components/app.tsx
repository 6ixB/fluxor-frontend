import { AppLayout } from "@/components/app-layout";
import { AppContent } from "@/components/app-content";

const App: React.FC = () => {
  return (
    <AppLayout>
      <AppContent />
    </AppLayout>
  );
};

export { App };
